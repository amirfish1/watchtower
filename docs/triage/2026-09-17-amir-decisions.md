# Amir Decision List — BECKY-* triage 2026-09-17

Nine tickets where triage (Jev, `scripts/jev-triage.py`) judged the parked
question genuinely needs **you**. Ordered most-confident first. Each shows the
decision, the agent's recommendation, and a line for your call.

---

**BECKY-DESIGN-1574** — Arketa-import residual hidden by bookingSource exclusion (conf 0.95)
Approve building Phase 1: a new additive, read-only predicate (`isUnverifiedPriorSystemDebt`) + a Becky-only tool surfacing 3 real uncollected imported appointments (Colista Torcellini, Jay Williams, Paul Rekieta) — touching nothing existing. Or accept status quo (3 rows, trending down, manual remedies).
Amir: **approve build / accept status quo**

**BECKY-DESIGN-1367** — LCPP recycled studio number: 45 junk texts/60 days (conf 0.93)
Doing nothing costs ~0.8 junk texts/day forever + the "reply STOP kills all real alerts" trap (already cost 3 weeks once; a matching text landed again 09-08). Build Phases 0+1 now (prevents self-text relay + STOP-trap), or still no-go?
Amir: **build P0+P1 / no-go**

**BECKY-1579** — Feature: low-balance alerts to instructors (conf 0.90)
Four scoping decisions: (1) channel — SMS/push, in-app list, or both; (2) trigger — reuse owner's low-balance threshold or separate instructor one; (3) scope — all instructors vs opt-in; (4) Body-Evolutions-only or platform-wide. No instructor-notification precedent exists in code.
Amir: **(1) __ (2) __ (3) __ (4) __**

**BECKY-1595** — Emergency contact feature: apply prod migration to finish (conf 0.81)
Feature code is done; the pending schema migration (`20260917050000_client_emergency_contact`) needs a human with real Postgres write access — the worker's DATABASE_URL/DIRECT_URL both fail auth. Run `npm run db:migrate:deploy` from apps/bookyourmat, or apply the SQL via the Supabase editor.
Amir: **run migration (then close)**

**BECKY-1489** — unpaid report's blanket import exclusion (conf 0.75)
Narrow `bookingSource==='import'` exclusion to genuinely pre-migration rows so post-import imported appointments become collectible — or keep them hidden as a separate dismissible bucket? Narrowing **reverses two of your own tested commits (2e54e4e66, 6ba5a134c)** that widened it; the agent lacks the incident context that motivated widening.
Amir: **narrow / keep + separate bucket / investigate incident first**

**BECKY-1494** — restoreCancelledAppointment sends nothing after a cancellation already reached the client (conf 0.71)
Send a corrective "your class is back on" SMS/email when restoring an appointment whose cancellation notice already fired? Currently deliberate no-message behavior (docstring added in b28e2191f) — fine for same-session undo, wrong when the client already saw a cancellation.
Amir: **add corrective message / keep silent**

**BECKY-1547** — Amy Powell Arketa 12-pack "missing" (conf 0.58 — Jev is torn)
Live data says the package is healthy (12 total = 6 applied + 6 reserved, zero overcommit); the ticket's premise matches the ORIGINAL Aug-10 state, already fixed 09-08. Agent can't run the ledger-verified dry run the acceptance criteria wants (no Arketa credentials on its VM).
Amir: **(a) close as stale false positive / (b) run verification from a Mac session first**

**BECKY-1567** — BE cancellations made in orphaned Arketa widget (Aug 10–Sep 10) never reached BYM (conf 0.57 — Jev is torn)
One-time reconciliation of Arketa cancellations vs BE's active bookings? Requires either re-touching the hard-retired Arketa sync (explicitly gated behind your sign-off — "DO NOT FLIP BACK ON WITHOUT THE OWNER") or Sonia manually exporting cancellations from Arketa's dashboard. Or accept reactive same-day staff handling as enough.
Amir: **reconcile via Sonia export / re-enable sync / accept risk, close**

**BECKY-DESIGN-1429** — override owner's "no payment texts" when her chosen channel fails (conf 0.26 — genuinely ambiguous)
Agent recommends all three: (1) one-time last-resort text when a payment alert is app-only AND the app notification actually failed; (2) let that one case skip quiet hours; (3) rename on-screen "Payment alerts" → "Payment texts". Plus: build the "never override me" opt-out now, or later?
Amir: **yes to all / pick pieces __ ; opt-out: now/later**

---

*Generated by `scripts/jev-triage.py` on 2026-09-17. Re-run after acting; tickets that stall again will reappear in the triage.*
